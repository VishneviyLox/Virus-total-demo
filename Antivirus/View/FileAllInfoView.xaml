<UserControl x:Class="Antivirus.View.FileAllInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Antivirus.View"
             xmlns:uc="clr-namespace:Antivirus.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Top">
            <uc:FileInfo/>
        </Grid>
        <Grid Grid.Row="1" Margin="0,-180,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">

                <ItemsControl ItemsSource="{Binding VirusDetections}">

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <uc:VirusInfoUserControl 
                                AntivirusName="{Binding AntivirusName}"
                                VirusName="{Binding VirusName}"
                                VirusType="{Binding VirusType}"/>

                            <DataTemplate.Resources>
                                <Style TargetType="Grid">
                                    <Setter Property="Margin" Value="0, 5, 0, 0"/>
                                </Style>
                            </DataTemplate.Resources>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                </ItemsControl>

            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
