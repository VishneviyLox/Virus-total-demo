<UserControl x:Class="Antivirus.UserControls.VirusInfoUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Antivirus.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <TextBlock Text="{Binding VirusName}"/>
        </Grid>

        <Grid>
            <TextBlock Text="{Binding VirusType}"/>
        </Grid>

        <Grid>
            <TextBlock Text="{Binding AntivirusName}"/>
        </Grid>

        <Grid>
            <Button Command="{Binding TestClickCommand}" Content="TEST"/>
            <TextBlock Text="{Binding IsVirus}"/>
        </Grid>
    </Grid>
</UserControl>
